# 后端 A 实测结果

测试日期：2026-09-06（本机日期）。

环境：Windows，Visual Studio Community 2026，MSVC 19.51.36252，x64。

## 编译和运行

执行 `build_and_test.bat`，使用 `/W4 /WX /O2` 编译，零警告、零编译错误。核心测试输出：

```text
Checks: 48, passed: 48, failed: 0
```

示例程序正常完成 31 个轴向切片，进度到达 100%，退出码为 0。

此外，CMake 已成功配置并生成 Visual Studio 工程，Release 模式成功编译后端静态库、测试程序和示例程序。

## 数值检查内容

| 检查项 | 结果 |
| --- | --- |
| 一维 FFT 与直接 DFT 对照 | 通过，最大误差小于 1e-11 |
| 二维 FFT 与直接二维 DFT 对照 | 通过，最大误差小于 1e-11 |
| 64、128、256、512 尺寸 FFT 正逆变换 | 通过，最大误差小于 1e-10 |
| ASM 倾斜平面波与解析解对照 | 通过，最大误差小于 1e-8 |
| 零距离传播 | 与输入完全一致 |
| ASM 正反传播、能量守恒、距离可加性 | 通过，针对无倏逝分量的测试场 |
| 倏逝分量处理 | 正向、反向均稳定滤除；零距离保留 |
| 光强归一化、全黑图 | 通过 |
| MSE 与 PSNR 手算参考值 | 通过 |
| 完全一致图像 | MSE=0，PSNR=正无穷 |
| 轴向剖面与双平面波干涉解析解对照 | 通过，最大误差小于 1e-9 |
| 单切片、中心行位置、统一归一化 | 通过 |
| 进度单调性、开始前及计算中取消 | 通过 |
| 18 组非法尺寸、参数或数据 | 均被明确拒绝 |

另逐行读取示例输出的 CSV，检查列数、行数和每个值：

| 输出 | 实际尺寸 | 数值检查 |
| --- | --- | --- |
| `build/input_intensity.csv` | 256×256 | 所有数值有限且在 0～1 |
| `build/reconstruction.csv` | 256×256 | 所有数值有限且在 0～1 |
| `build/axial.csv` | 256×31 | 所有数值有限且在 0～1 |

这些结果验证了独立后端 A 模块。MFC 界面和后端 B 的 GS、快速搜索不在本次实现范围内，整合后的完整程序仍需全组联调。
